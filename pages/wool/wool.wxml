<view class="container">
    <scroll-view scroll-y class="flower-list">
        <block wx:for="{{flowers}}" wx:key="index">
            <button class="flower-item" bindtap="onFlowerTap" data-index="{{index}}">
                {{item}}
            </button>
        </block>
    </scroll-view>
</view>